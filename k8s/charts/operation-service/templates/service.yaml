apiVersion: v1
kind: Service
metadata:
  name: {{ include "operation-service.fullname" . }}
  labels:
    {{- include "operation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "operation-service.selectorLabels" . | nindent 4 }}
