apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "campaign-service.fullname" . }}-test-connection"
  labels:
    {{- include "campaign-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "campaign-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
