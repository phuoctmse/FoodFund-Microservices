import { Module } from "@nestjs/common"

@Module({})
export class PrismaModule {
    // This module is kept for backward compatibility
    // Use PrismaService as a base class for your specific Prisma clients
}
